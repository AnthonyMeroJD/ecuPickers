package com.example.ecupickers.factory

import com.example.ecupickers.constantes.EnumReferenciasDB
import com.google.firebase.database.DatabaseReference
import com.google.firebase.database.FirebaseDatabase

object DbReference {/*
    val db:FirebaseDatabase= FirebaseDatabase.getInstance()
    fun getRef(referencia:EnumReferenciasDB):DatabaseReference{
        return when(referencia.rutaDB()){
            EnumReferenciasDB.CATEGORIA.rutaDB()->db.reference.child(referencia.rutaDB())
            EnumReferenciasDB.MIEMBROSALIMENTOS.rutaDB()->db.reference.child(referencia.rutaDB())
            EnumReferenciasDB.PRODUCTO.rutaDB()->db.reference.child(referencia.rutaDB())
            else -> db.reference.child("Categoria")
        }
    }*/
}