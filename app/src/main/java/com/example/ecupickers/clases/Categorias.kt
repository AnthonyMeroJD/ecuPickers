package com.example.ecupickers.clases

import com.example.ecupickers.constantes.EnumReferenciasDB
import com.example.ecupickers.factory.DbReference
import com.google.firebase.database.Query

class Categorias {
/*
    fun traerCategorias(limit:Int=12): Query {
       val ref= DbReference.getRef(EnumReferenciasDB.CATEGORIA).child("CategoriaRestaurante")
       val qry=ref.limitToFirst(limit)
        return qry
    }*/
}